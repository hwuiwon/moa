# Goal Card: Context Compaction Under Token Pressure

Drive a long session with enough turns and early tool errors to force a checkpoint under a low test threshold. After compaction, the agent must still remember the earlier file-not-found and zero-match edit errors.
