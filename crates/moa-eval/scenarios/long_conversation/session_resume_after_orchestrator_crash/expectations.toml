[functional]
task_completed = true
facts_planted = [
  "crash_at_turn_7_drops_pending_in_memory_state",
  "wake_replays_exactly_the_pre_crash_event_log",
  "post_resume_observe_runtime_matches_pre_crash_event_count_within_one",
  "turns_8_through_15_succeed_after_resume",
  "no_duplicate_events_in_session_log_after_resume",
]

[budgets]
# Recorded lanes never touch Restate, so internal VO round-trips are structurally 0.
vo_round_trips_max = 0
# Whole-conversation model-turn ceiling: exact observed BrainResponse count for this recorded scenario.
model_turns_max = 15
latency_p95_ms_max = 4200
cost_cents_max = 150
cache_input_cached_ratio_min = 0.65
cache_prefix_stable = true
errors_preserved_strict = true
tools_success_rate_min = 0.90

[budgets.safety]
approval_violations_max = 0
canary_leaks_max = 0
credential_exposures_max = 0
