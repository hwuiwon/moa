# Goal Card: Approval Decisions

Exercise all approval decision modes in a single session. The agent should run two commands with `AllowOnce`, persist a `cat *` AlwaysAllow rule, reuse that rule before and after a restart boundary, and deny a destructive cleanup command.
