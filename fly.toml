app = "moa-brains"
primary_region = "iad"
kill_signal = "SIGTERM"
kill_timeout = "30s"

[build]
  dockerfile = "Dockerfile"

[env]
  MOA_CLOUD_ENABLED = "true"
  MOA_CLOUD_HANDS_DEFAULT_PROVIDER = "local"
  MOA_LOCAL_MEMORY_DIR = "/data/memory"
  MOA_LOCAL_SANDBOX_DIR = "/data/sandbox"
  MOA_CLOUD_MEMORY_DIR = "/data/memory"
  MOA_CLOUD_FLYIO_INTERNAL_PORT = "8080"
  MOA_CLOUD_FLYIO_HEALTH_BIND = "0.0.0.0"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "suspend"
  auto_start_machines = true
  min_machines_running = 0

  [[http_service.checks]]
    grace_period = "10s"
    interval = "30s"
    method = "GET"
    timeout = "5s"
    path = "/health"

[[mounts]]
  source = "moa_data"
  destination = "/data"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
